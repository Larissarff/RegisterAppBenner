<UserControl x:Class="RegisterAppBenner.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:RegisterAppBenner.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModels:ProductViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Label Content="Nome:" Width="80" VerticalAlignment="Center"/>
            <TextBox Width="180" Margin="0 0 20 0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Código:" Width="70" VerticalAlignment="Center"/>
            <TextBox Width="120" Margin="0 0 20 0" Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Preço:" Width="60" VerticalAlignment="Center"/>
            <TextBox Width="100" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Adicionar" Width="100" Margin="0 0 10 0"
                    Click="AddProduct_Click"/>
            <Button Content="Atualizar" Width="100" Margin="0 0 10 0"
                    Click="UpdateProduct_Click"/>
            <Button Content="Excluir" Width="100"
                    Click="DeleteProduct_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Products}" 
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  Margin="0,10,0,0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Code}" Width="100"/>
                <DataGridTextColumn Header="Preço (R$)" Binding="{Binding Price}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
