<UserControl x:Class="RegisterAppBenner.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RegisterAppBenner.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">


	<UserControl.DataContext>
		<vm:OrderViewModel/>
	</UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Cadastro de Pedidos"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="10">

                <TextBlock Text="Cliente:" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Persons}"
                          SelectedItem="{Binding SelectedPerson}"
                          DisplayMemberPath="Name"
                          Margin="0 5 0 10"/>

                <TextBlock Text="Produto:" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          DisplayMemberPath="Name"
                          Margin="0 5 0 10"/>

                <TextBlock Text="Quantidade:" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 5 0 10"/>

                <Button Content="Adicionar Item"
                        Click="AddItem_Click"
                        Background="#2E8B57"
                        Foreground="White"
                        Padding="6"
                        Margin="0 10 0 10"/>

                <DataGrid ItemsSource="{Binding OrderItems}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          Margin="0 10 0 10"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Produto" Binding="{Binding Product.Name}" Width="*"/>
                        <DataGridTextColumn Header="Código" Binding="{Binding Product.Code}" Width="100"/>
                        <DataGridTextColumn Header="Preço (R$)" Binding="{Binding Product.Price, StringFormat={}{0:N2}}" Width="100"/>
                        <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantity}" Width="100"/>
                        <DataGridTextColumn Header="Subtotal (R$)" Binding="{Binding Subtotal, StringFormat={}{0:N2}}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Total: " FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalValue, StringFormat={}{0:C2}}" 
                               FontSize="16" FontWeight="Bold" Foreground="DarkGreen"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10">
                <TextBlock Text="Forma de Pagamento:" FontWeight="SemiBold"/>
                <ComboBox SelectedItem="{Binding SelectedPaymentMethod}"
                          ItemsSource="{Binding PaymentMethods}"
                          Margin="0 5 0 20"/>

                <Button Content="Finalizar Pedido"
                        Click="FinalizeOrder_Click"
                        Background="#0078D7"
                        Foreground="White"
                        Padding="8"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="10">
                <TextBlock Text="Pedidos Salvos" FontWeight="SemiBold" Margin="0 0 0 5"/>
                <DataGrid ItemsSource="{Binding Orders}"
                          SelectedItem="{Binding SelectedOrder}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          Height="450">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding Person.Name}" Width="150"/>
                        <DataGridTextColumn Header="Total (R$)" Binding="{Binding TotalValue, StringFormat={}{0:N2}}" Width="100"/>
                        <DataGridTextColumn Header="Data" Binding="{Binding SaleDate, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>